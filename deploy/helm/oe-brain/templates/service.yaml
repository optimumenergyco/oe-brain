apiVersion: v1
kind: Service
metadata:
  name: {{ include "oe-brain.fullname" . }}
  labels:
    {{- include "oe-brain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "oe-brain.selectorLabels" . | nindent 4 }}
